version: '3.8'

services:
  eve:
    container_name: eve
    build: .
    restart: always
    command: bash -c "python3 -u setup.py eve --host ${DB_HOST} --worker_size ${WORKER_SIZE} --worker_id ${WORKER_ID}"
    volumes:
      - .:/app

  
